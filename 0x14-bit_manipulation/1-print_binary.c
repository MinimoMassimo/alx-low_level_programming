#include "main.h"

/**
 * print_binary - prints binary rep of a num
 * @n: the decimal num
 * Return: nothing
 */
void print_binary(unsigned long int n)
{
	int index = 1;
	int i = 0;
	int ans = 0;

	while (n / 2 > 0)
	{
		ans  += (n % 2) *  
		n /= 2;
	}
}
